name: Test Call Reusable Workflow

on:
  workflow_dispatch:

jobs:
  call-reusable:
    uses: k-candidate/gha-workflows/.github/workflows/test-reusable.yaml@test-env
    secrets:
      caller_secret: ${{ secrets.TEST_CALLER_SECRET }}
      # Note: NOT passing TEST_SECRET from environment
      # The reusable workflow should get it from its own environment
